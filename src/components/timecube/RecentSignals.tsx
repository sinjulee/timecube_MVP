import { Loader } from 'lucide-react';import { RecentSignal } from '../../types/timecube';import { signalStyles } from './statusStyles';
export default function RecentSignals({signals}:{signals:RecentSignal[]}){return <section className='bg-white border border-tc-line p-8 rounded-xl'><h3>Recent Signals</h3>{signals.map(s=>{const Icon=s.icon;const st=signalStyles[s.status];return <div key={s.id} className={`flex justify-between p-4 border rounded-xl ${st.wrapper}`}><div className={`w-10 h-10 rounded-full flex items-center justify-center ${st.iconBg} ${st.iconColor}`}><Icon size={20}/></div><div>{s.title}</div>{s.status==='retry_waiting'&&<Loader className='animate-spin'/>}</div>})}</section>}
